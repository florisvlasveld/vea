# === API Keys ===

# OpenAI API key (required if using OpenAI models like o4-mini)
OPENAI_API_KEY=sk-...

# Anthropic Claude API key (optional, for Claude models like claude-3-7-sonnet-latest)
ANTHROPIC_API_KEY=...

# Google Gemini API key (optional, for gemini-pro and other Gemini models)
GEMINI_API_KEY=...

# Slack Bot token (typically starts with xoxb-)
SLACK_TOKEN=xoxb-...

# Todoist API token (used to fetch tasks)
TODOIST_TOKEN=...

# === Personalization ===

# Your email address, used to filter out declined calendar invites
GOOGLE_ACCOUNT_EMAIL=you@example.com

# A short personal bio, included in the prompt for context (optional)
BIO="John is the CTO at AI Corp. He cares about making engineering teams successful."

# === Calendar settings ===

# Optional: comma-separated list of additional Google Calendar IDs to include
GCAL_ADDITIONAL_CALENDARS=someone_else@gmail.com,shared_calendar@group.calendar.google.com

# Optional: blacklist certain calendar events by substring match
# Comma-separated list (e.g., 'Focus time, Travel, Lunch')
CALENDAR_EVENT_BLACKLIST=Focus time, Travel, Lunch

# === Output settings ===

# Optional: default path to save the output if --save-path is not passed
SAVE_PATH=~/DailyBrief/

# === Model selection ===

# You can also pass the model via CLI (--model), but you can set a default here
DEFAULT_MODEL=gemini-2.5-pro-preview-05-06

# === Logging ===

# Set to '1' to enable debug logs by default (can also use --debug CLI flag)
DEBUG=0